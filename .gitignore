# ── Dependencies & Build ──────────────────────────────────────────────────────
node_modules/
build/
dist/
coverage/

# ── Environment ───────────────────────────────────────────────────────────────
.env*
!.env.example

# ── OS & Editor Noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
*.swp
*.swo
.vscode/settings.json

# ── AI Tooling (Antigravity IDE / Agent Workspace) ────────────────────────────
# Agent skill definitions and rules (treated as local/personal)
.agents/

# Impeccable design sidecar (AI-generated design tokens & component snippets)
.impeccable/

# Fallow — AI binary caches and graph indexes (never commit)
.fallow/

# AI-generated product & design documentation (optional: remove these two lines
# if you want PRODUCT.md and DESIGN.md tracked in git)
PRODUCT.md
DESIGN.md

# Root metadata file generated by Antigravity IDE
metadata.json

# Gemini / Antigravity local config folder (if ever placed in workspace)
.gemini/
